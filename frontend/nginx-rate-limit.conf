# Rate limiting zones — included at http level by nginx
# 30 requests/sec per IP with burst capacity
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=conn:10m;
