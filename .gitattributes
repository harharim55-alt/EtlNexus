# Shell scripts must keep LF endings or they break in Linux containers
# (a CRLF shebang makes the kernel look for "bash\r"). Force LF regardless of
# the host's core.autocrlf setting.
*.sh text eol=lf
dev/spark/entrypoint.sh text eol=lf
frontend/docker-entrypoint.sh text eol=lf
