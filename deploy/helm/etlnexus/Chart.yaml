apiVersion: v2
name: etlnexus
description: EtlNexus — data-products catalog (FastAPI backend + React/nginx frontend)
type: application
# Chart version — bump when the chart templates change.
version: 0.1.0
# App version — the image version this chart defaults to.
appVersion: "0.11.0"
