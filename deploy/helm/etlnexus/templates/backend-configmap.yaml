apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "etlnexus.fullname" . }}-backend-config
  labels: {{- include "etlnexus.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.backend.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
